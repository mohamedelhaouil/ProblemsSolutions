/**
 *
 *    author:  MEL05
 *
**/
#include<bits/stdc++.h>

using namespace std;

typedef long long ll;
const int N = 4e5 + 3;
int p[N];
int main () {
	ios_base :: sync_with_stdio(false);
	cin.tie(0);
	int t;
	cin >> t;
	while(t--) {
		int n;
		cin >> n;
		for(int i = 0; i < n; i++) {
			cin >> n;
		}
		if(n < 6 || p[0] < 3) {
			cout << 0 << " " << 0 << " " << 0 << endl;
			continue;
		}
		bool bl = true;
		for(int i = 0; i < n; i++) {
			
		}
	}
	return 0;
}

